use patternfly_yew::prelude::PageSection;
use yew::prelude::*;

/// Placeholder for the `/credential-definitions` route.
#[function_component(CredentialDefinitions)]
pub fn credential_definitions() -> Html {
    html! {
        <PageSection>
            <p>{ "TODO" }</p>
        </PageSection>
    }
}
