use patternfly_yew::prelude::PageSection;
use yew::prelude::*;

/// Placeholder for the `/credentials` (credentials & issuance) route.
#[function_component(Credentials)]
pub fn credentials() -> Html {
    html! {
        <PageSection>
            <p>{ "TODO" }</p>
        </PageSection>
    }
}
